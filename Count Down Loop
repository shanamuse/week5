#include <stdio.h>

int main() {
    for (int i = 70; i >= 0; i -= 2) {
        printf("%d", i);  

        if (i > 0) {
            printf(", "); 
        }
    }

    printf("\n");

    return 0;
}
