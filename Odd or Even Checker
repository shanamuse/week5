#include <stdio.h>

int main() {
    int number;

    printf("Please enter a whole number:\n");
    scanf("%d", &number);

    printf("%d is %s.\n", number, (number % 2 == 0) ? "even" : "odd");

    return 0;
}
