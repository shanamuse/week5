#include <stdio.h>

int main() {
    int input;

    printf("?\n");
    scanf("%d", &input);

    for (int i = 1; i <= input; i++) {
        printf("1 / %d is %.6f\n", i, 1.0 / i);
    }

    return 0;
}
